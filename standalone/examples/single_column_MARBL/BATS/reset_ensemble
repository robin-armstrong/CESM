ENS_BACKUP_DIR=/glade/u/home/rarmstrong/marbldart_ensemble_backup

if [ -d "${ENS_BACKUP_DIR}/temp" ]; then
	rm -rf ensemble; cp -r ${ENS_BACKUP_DIR}/temp ensemble; rm -f ensemble/README.txt
else
	echo "ERROR: directory '${ENS_BACKUP_DIR}/temp' must exist."
fi
